import {
  Home as FilledHome,
  Folder as FilledFolder,
  Database as FilledDatabase,
  Download as FilledDownload,
  Settings as FilledSettings,
} from '@/components/icons/flora';

export { FilledHome, FilledFolder, FilledDatabase, FilledDownload, FilledSettings };
